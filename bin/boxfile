#!/usr/bin/env bash
# -*- mode: bash; tab-width: 2; -*-
# vim: ts=2 sw=2 ft=bash noet

# $1 = payload JSON

# exit if anything fails
set -e

cat <<END
dev:
  fs_watch: true
  
build:
  lib_dirs:
    - node_modules
    - bower_components
    
web1:
  exec: nginx
END
