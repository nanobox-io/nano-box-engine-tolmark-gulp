#!/usr/bin/env bash
# -*- mode: bash; tab-width: 2; -*-
# vim: ts=2 sw=2 ft=bash noet

# $1 = payload JSON

# exit if anything fails
set -e

cat <<END
run.config:
	fs_watch: true
  cache_dirs:
    - node_modules
    - bower_components
    - server
    - rel
    
web.site:
  start: nginx
END
